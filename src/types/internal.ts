export type journeyAccommodationPricingMapType = {
  departureCode: string;
  arrivalCode: string;
  date: string;
  departureTime: string;
  arrivalTime: string;
  accomodation: string;
  adultPrice: number;
  childPrice: number;
};
