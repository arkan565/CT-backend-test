export type journeyDestination = {
  destinationCode: string;
  destinationTree: string[];
  arrivalCode: string;
  arrivalTree: string[];
};

export type supplierStationCorrelation = {
  code: string;
  suppliers: string[];
};
